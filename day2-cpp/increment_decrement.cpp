#include <bits/stdc++.h>
using namespace std;

int main()
{

    // int a = 10;
    // a = a + 1; // a++ || ++a;
    // ++a; // Pre increment:
    // a++; // Post increment:
    // cout << a++ + ++a + a++;

    int a = 1;

    cout << a++ + --a + a++;

    return 0;
}